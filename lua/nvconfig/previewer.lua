  --[[ This file is only for preview purpose.
       This is how comments look like. ]]
local Name = "String"
local Bools = { true, false }
local Numbers = { 1, 2, 3, 4, 5 }
local Lua = { type, typeof, assert, setmetatable, table, self }
local function function(x)
  if x = nil or x < 1 then
    return false
  end
  return true
end
local Part = workspace:FindFirstChild("Part")
if Part then
  if i = 1, 5 do
  Part.Color = Color3.new(1, 0, 0)
  task.wait(.10)
  Part.Color = Color3.new()
  task.wait(.10)
end
  Part:Destory()
end
print(#game:GetService("Players"):GetPlayers())
